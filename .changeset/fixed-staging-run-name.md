---
"web-app-foundation": patch
---

Fix Fixed Staging publisher trust validation so dynamic workflow `run-name` values are not confused with the static request workflow identity selected by the `workflow_run` trigger.

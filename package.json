{
  "name": "web-app-foundation",
  "version": "0.1.0",
  "private": true,
  "description": "AI-first shared foundation for web application development.",
  "engines": {
    "node": "^24.20.0 || >=26.0.0"
  },
  "scripts": {
    "foundation:validate": "node scripts/validate-foundation.mjs",
    "foundation:test": "node scripts/test-foundation-validator.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "version:status": "changeset status",
    "tag-version": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.2"
  }
}

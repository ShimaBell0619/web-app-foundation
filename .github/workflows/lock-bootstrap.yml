name: Temporary lockfile bootstrap

on:
  push:
    branches: [fix/astra-review-hardening]

permissions:
  contents: write

jobs:
  generate:
    if: github.actor == 'ShimaBell0619' && !contains(github.event.head_commit.message, 'chore: generate locked release tooling')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
      - name: Generate package lock
        run: npm install --package-lock-only --ignore-scripts
      - name: Commit generated lockfile
        shell: bash
        run: |
          if git diff --quiet -- package-lock.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "chore: generate locked release tooling"
          git push origin HEAD:${GITHUB_REF_NAME}
